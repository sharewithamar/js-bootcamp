// === equality operator
// !== not equal operator
// < lesser than
// > lesser than
// <==
// >==

let temp = 21;

//let isFreezing = temp === 31;
//let isFreezing = temp !== 31;
//let isFreezing = temp < 31;
//let isFreezing = temp > 31;
//let isFreezing = temp <= 31;
//let isFreezing = temp >= 31;

if (temp >= 31) {
  console.log('it is freezing outside !');
}

//console.log(isFreezing);

//challenge area

let age = 79;
let isChild = age <= 7;
let isSenior = age >= 65;

if (age <= 7) console.log('You are a child , you have kids discount ');
if (age >= 65)
  console.log('You are an elder citizen , you have kids discount ');

// if <7 child discount
//if >65 discount
